import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    footer: {
        flex: 2,
        justifyContent: 'center',
        paddingLeft: 10,
        paddingRight: 10
    },
    btn: {
        height: 60,
        justifyContent: 'center',
        alignItems: 'center',
        marginTop: 20,
        marginHorizontal: 50,
        borderRadius: 10,
    }

})