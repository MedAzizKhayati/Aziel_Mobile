import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    footer: {
        flex: 2,
        justifyContent: 'center',
        paddingLeft: 10,
        paddingRight: 10
    }

})